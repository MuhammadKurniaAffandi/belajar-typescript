describe("Data Type", function () {
  it("should must declare", () => {
    let name: string = "Muhammad Kurnia Affandi";
    let balance: number = 100;
    let isVip: boolean = true;

    console.info(name);
    console.info(balance);
    console.info(isVip);
  });
});
