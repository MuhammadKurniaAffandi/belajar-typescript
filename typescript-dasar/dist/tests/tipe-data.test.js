"use strict";
describe("Data Type", function () {
    it("should must declare", () => {
        let name = "Muhammad Kurnia Affandi";
        let balance = 100;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
